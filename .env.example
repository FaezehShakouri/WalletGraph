# Copy to .env; never commit .env or paste deploy keys into chat.
GRAPH_DEPLOY_KEY=
ETHEREUM_SUBGRAPH_SLUG=
BASE_SUBGRAPH_SLUG=
SUBGRAPH_VERSION=v0.1.0
# Studio query URLs shown after deployment; may include a provider API key.
ETHEREUM_SUBGRAPH_URL=
BASE_SUBGRAPH_URL=
# Optional authenticated RPCs for evidence refresh (public fallbacks otherwise).
ETHEREUM_RPC_URL=
BASE_RPC_URL=
